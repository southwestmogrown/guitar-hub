import React from 'react';

function Comments(props) {
    // console.log(props.comments[0].body)
    return (
        <div>
            {/* <p>{props.comments[0].body}</p> */}
        </div>
    )
}

export default Comments;
